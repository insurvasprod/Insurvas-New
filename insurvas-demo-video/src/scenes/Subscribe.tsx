import React from "react";
import { useCurrentFrame } from "remotion";
import { Background, BrowserFrame, Callout, Eyebrow, Progress, Check, colors, enter, pop } from "./shared";

export const Subscribe: React.FC = () => { const frame = useCurrentFrame(); const advanced = frame > 68; return <Background><Progress step={2} /><div style={{ position: "absolute", left: 66, top: 73, ...enter(frame, 6) }}><Eyebrow>02 · Select a plan</Eyebrow><div style={{ marginTop: 11, fontSize: 34, fontWeight: 800 }}>Pick the tools your team needs.</div></div><div style={{ position: "absolute", left: 128, top: 178, ...enter(frame, 14) }}><BrowserFrame label="plans" light><div style={{ padding: "40px 50px" }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "end" }}><div><div style={{ fontSize: 25, fontWeight: 800, color: colors.navy }}>Choose your plan</div><div style={{ marginTop: 6, color: colors.muted, fontSize: 14 }}>Change plans as your business grows.</div></div><div style={{ padding: 4, borderRadius: 9, background: "#edf3f8", display: "flex", gap: 3, fontSize: 12, fontWeight: 700 }}><div style={{ padding: "8px 13px", borderRadius: 6, background: colors.white, color: colors.blue }}>Monthly</div><div style={{ padding: "8px 13px", color: colors.muted }}>Yearly · save 15%</div></div></div><div style={{ display: "flex", gap: 18, marginTop: 30 }}><Plan name="Basic" price="$249" desc="For focused individual teams" selected={!advanced} features={["Lead workspace", "Policy tracking", "Core reporting"]} /><Plan name="Advanced" price="$449" desc="For teams ready to scale" selected={advanced} features={["Everything in Basic", "Dialer & automations", "Advanced reporting"]} featured /></div><div style={{ marginTop: 20, display: "flex", justifyContent: "space-between", alignItems: "center" }}><div style={{ color: colors.muted, fontSize: 12 }}>14-day trial · cancel anytime</div><div style={{ padding: "11px 24px", borderRadius: 8, background: colors.blue, color: colors.white, fontSize: 14, fontWeight: 800 }}>Continue to secure checkout →</div></div></div></BrowserFrame><Callout title="Transparent access" body="Features and access are tied to the selected plan." top={195} right={-292} /><div style={{ position: "absolute", top: 424, right: -298, ...pop(frame, 75) }}><Check>{advanced ? "Advanced plan selected" : "Basic plan selected"}</Check></div></div></Background>; };

const Plan: React.FC<{ name: string; price: string; desc: string; selected: boolean; features: string[]; featured?: boolean }> = ({ name, price, desc, selected, features, featured = false }) => <div style={{ flex: 1, minHeight: 242, padding: 20, borderRadius: 12, border: `2px solid ${selected ? colors.blue : colors.line}`, background: selected ? "#f5fbff" : colors.white, position: "relative", boxShadow: selected ? "0 10px 22px rgba(0,112,204,.1)" : "none" }}>{featured && <div style={{ position: "absolute", right: 14, top: 14, color: colors.blue, fontSize: 10, fontWeight: 900, letterSpacing: 1, textTransform: "uppercase" }}>Most popular</div>}<div style={{ fontSize: 20, fontWeight: 800, color: colors.navy }}>{name}</div><div style={{ marginTop: 14, color: colors.navy, fontSize: 29, fontWeight: 800 }}>{price}<span style={{ fontSize: 13, fontWeight: 500, color: colors.muted }}>/month</span></div><div style={{ marginTop: 4, color: colors.muted, fontSize: 12 }}>{desc}</div><div style={{ display: "grid", gap: 8, marginTop: 17 }}>{features.map((feature) => <Check key={feature}>{feature}</Check>)}</div></div>;
