import React from "react";
import { useCurrentFrame } from "remotion";
import { Background, BrowserFrame, Callout, Eyebrow, Progress, colors, enter, pop } from "./shared";

export const Access: React.FC = () => { const frame = useCurrentFrame(); const signed = frame > 86; return <Background><Progress step={3} /><div style={{ position: "absolute", left: 66, top: 73, ...enter(frame, 6) }}><Eyebrow>03 · Access granted</Eyebrow><div style={{ marginTop: 11, fontSize: 34, fontWeight: 800 }}>Your workspace is ready.</div></div><div style={{ position: "absolute", left: 245, top: 185, ...enter(frame, 15) }}><BrowserFrame label="app/login" light><div style={{ minHeight: 470, display: "grid", placeItems: "center", background: "linear-gradient(160deg,#fafdff,#edf7ff)" }}><div style={{ width: 310, padding: 29, background: colors.white, borderRadius: 15, border: `1px solid ${colors.line}`, boxShadow: "0 16px 40px rgba(14,45,74,.09)" }}><div style={{ textAlign: "center" }}><div style={{ color: colors.blue, fontWeight: 900, fontSize: 23 }}>Insurvas</div><div style={{ marginTop: 6, color: colors.muted, fontSize: 13 }}>Sign in to your account</div></div><Field label="Email" value="amara@northstarins.com" active={frame > 28} /><Field label="Password" value="••••••••••••" active={frame > 45} /><div style={{ marginTop: 19, height: 39, borderRadius: 8, background: colors.blue, color: colors.white, display: "grid", placeItems: "center", fontWeight: 800, fontSize: 14, opacity: signed ? 1 : .76 }}>Sign in</div></div></div></BrowserFrame><Callout title="One secure entry" body="The user signs in and is routed into their tenant workspace." top={184} right={-275} /><div style={{ position: "absolute", top: 400, right: -273, ...pop(frame, 93), color: "#b5f5d6", fontSize: 20, fontWeight: 800 }}>✓ Access confirmed</div></div></Background>; };

const Field: React.FC<{ label: string; value: string; active: boolean }> = ({ label, value, active }) => <div style={{ marginTop: 18 }}><div style={{ fontSize: 12, fontWeight: 700, color: colors.text, marginBottom: 6 }}>{label}</div><div style={{ height: 38, border: `1px solid ${active ? colors.blue : colors.line}`, borderRadius: 7, display: "flex", alignItems: "center", padding: "0 10px", fontSize: 12, color: colors.text, boxShadow: active ? "0 0 0 3px rgba(0,112,204,.08)" : "none" }}>{value}</div></div>;
