import React from "react";
import { useCurrentFrame } from "remotion";
import { Background, BrowserFrame, Callout, Eyebrow, Progress, colors, enter, pop } from "./shared";

export const Signup: React.FC = () => { const frame = useCurrentFrame(); const typed = frame > 45; return <Background><Progress step={1} /><div style={{ position: "absolute", left: 66, top: 73, ...enter(frame, 6) }}><Eyebrow>01 · Get started</Eyebrow><div style={{ marginTop: 11, fontSize: 34, fontWeight: 800 }}>Choose your starting point.</div></div><div style={{ position: "absolute", left: 210, top: 184, ...enter(frame, 16) }}><BrowserFrame label="signup" light><div style={{ display: "flex", minHeight: 470 }}><div style={{ width: 285, padding: "56px 30px", background: "linear-gradient(160deg,#e9f5ff,#cce8ff)", borderRight: `1px solid ${colors.line}` }}><div style={{ color: colors.blue, fontWeight: 900, fontSize: 23 }}>Insurvas</div><div style={{ marginTop: 64, fontSize: 28, fontWeight: 800, lineHeight: 1.12, color: colors.navy }}>Your operation.<br />Under control.</div><div style={{ marginTop: 20, color: colors.muted, lineHeight: 1.5, fontSize: 15 }}>One workspace for leads, policies, and the daily work behind every sale.</div></div><div style={{ flex: 1, padding: "44px 52px" }}><div style={{ color: colors.muted, fontSize: 13, fontWeight: 700 }}>STEP 1 OF 2</div><div style={{ marginTop: 8, fontSize: 30, fontWeight: 800, color: colors.navy }}>Create your account</div><div style={{ marginTop: 7, fontSize: 15, color: colors.muted }}>Start your 14-day workspace trial.</div><div style={{ marginTop: 25, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>{["Full name", "Work email", "Business name", "Password"].map((label, index) => <div key={label} style={{ gridColumn: index === 2 || index === 3 ? "span 2" : undefined }}><div style={{ fontSize: 13, fontWeight: 700, color: colors.text, marginBottom: 7 }}>{label}</div><div style={{ height: 39, border: `1px solid ${typed && index < 3 ? colors.blue : colors.line}`, borderRadius: 8, padding: "0 12px", display: "flex", alignItems: "center", color: typed && index < 3 ? colors.text : "#a5b1bd", fontSize: 14, boxShadow: typed && index < 3 ? "0 0 0 3px rgba(0,112,204,.08)" : "none" }}>{typed && index < 3 ? ["Amara Davis", "amara@northstarins.com", "Northstar Life Group"][index] : `Enter ${label.toLowerCase()}`}</div></div>)}</div><div style={{ marginTop: 25, height: 41, borderRadius: 8, background: colors.blue, color: colors.white, display: "grid", placeItems: "center", fontSize: 15, fontWeight: 800, opacity: frame > 82 ? 1 : .72 }}>Continue to plans <span style={{ display: "inline-block", marginLeft: 6 }}>→</span></div></div></div></BrowserFrame><Callout title="Simple onboarding" body="Create the owner account and business workspace in one guided flow." top={236} right={-275} /><div style={{ position: "absolute", top: 382, right: -272, ...pop(frame, 90), color: "#86d8ff", fontSize: 27, fontWeight: 800 }}>✓ Account created</div></div></Background>; };
