import React from "react";
import { useCurrentFrame } from "remotion";
import { Background, Eyebrow, Check, colors, enter, pop } from "./shared";

export const Outro: React.FC = () => { const frame = useCurrentFrame(); return <Background><div style={{ position: "absolute", inset: 0, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}><div style={{ ...enter(frame, 5) }}><Eyebrow>Insurvas in one sentence</Eyebrow></div><div style={{ ...enter(frame, 16, 24), marginTop: 20, fontSize: 45, lineHeight: 1.08, fontWeight: 800, textAlign: "center" }}>Subscribe once.<br />Get access. Run the operation.</div><div style={{ ...enter(frame, 35), marginTop: 30, display: "grid", gap: 12 }}><Check>Choose a plan that fits the team</Check><Check>Activate a secure tenant workspace</Check><Check>Work from one operational dashboard</Check></div><div style={{ ...pop(frame, 73), marginTop: 35, padding: "13px 25px", borderRadius: 9, background: colors.blue, color: colors.white, fontSize: 15, fontWeight: 800 }}>Ready for a deeper walkthrough →</div></div><div style={{ position: "absolute", bottom: 30, width: "100%", textAlign: "center", fontSize: 13, color: "#8ab8d6", letterSpacing: 1.5 }}>INSURVAS · INSURANCE OPERATIONS, CONNECTED</div></Background>; };
